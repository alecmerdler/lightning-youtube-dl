# lightning-youtube-dl

This project provides a simple Lightning component wrapper for the popular `youtube-dl` tool.

## Usage

TODO(alecmerdler): Provide basic usage of the `YoutubeDL` component API.
